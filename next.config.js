/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "lh3.googleusercontent.com",
      "plus.unsplash.com",
      "images.unsplash.com",
    ],
  },
};

module.exports = nextConfig;
